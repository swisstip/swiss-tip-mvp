# Placeholder: reserves this workflow's name and its "Run workflow" button on the
# default branch. The real workflow arrives with the commit "Check and ship the packs";
# until then, choose a branch that carries it under "Use workflow from".
name: Container images
on: workflow_dispatch
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Placeholder only - choose a branch that carries the real workflow."
