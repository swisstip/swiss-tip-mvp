# The context is this directory, which holds what belongs to the pack: the
# welcome panel and the image README. The configuration, start script, shim,
# health check and plugin come from the named context "shared", the code
# repository's docker/opencode; the OpenCode binary from the npm registry,
# and the knowledge release from the base image.
*
!welcome.json
!README.md
